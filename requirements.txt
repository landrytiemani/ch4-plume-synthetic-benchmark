numpy>=1.26
pandas>=2.2
rasterio>=1.3
matplotlib>=3.8
torch>=2.2
tqdm>=4.66
requests>=2.31

# Optional -- data acquisition only (not needed once chips are downloaded):
#   pip install -e ".[acquire]"     # Google Earth Engine (GEE chip export)
#   pip install -e ".[labels]"      # NASA earthaccess (EMIT download)